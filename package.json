{
  "name": "krowdco-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/mobile",
    "apps/api",
    "packages/shared"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web",
    "build:api": "npm run build --workspace=apps/api",
    "build:web": "npm run build --workspace=apps/web",
    "lint": "npm run lint --workspaces --if-present",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\"",
    "db:generate": "npm run db:generate --workspace=apps/api",
    "db:migrate": "npm run db:migrate --workspace=apps/api",
    "test": "npm run test --workspaces --if-present",
    "deploy:production": "powershell -ExecutionPolicy Bypass -File scripts/deploy-to-production.ps1",
    "deploy:check": "powershell -ExecutionPolicy Bypass -File scripts/check-deploy.ps1",
    "deploy:health": "powershell -ExecutionPolicy Bypass -File scripts/health-check.ps1",
    "deploy:rollback": "powershell -ExecutionPolicy Bypass -File scripts/rollback-production.ps1"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "fast-check": "^3.22.0"
  },
  "overrides": {
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "styled-jsx": "5.1.1"
  }
}
